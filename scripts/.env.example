# Sui Network Configuration
# Options: mainnet, testnet, devnet, localnet
SUI_NETWORK=testnet

# Package ID (after publishing the Move package)
PACKAGE_ID=0x...

# Board Registry ID (shared object created during init)
BOARD_REGISTRY_ID=0x...

# User Secret Key (your wallet private key in base64 or hex format)
# NEVER commit this to version control!
USER_SECRET_KEY=suiprivkey...

# Admin Address (for admin operations)
ADMIN_ADDRESS=0x...

# Optional: Custom RPC URL
# SUI_RPC_URL=https://fullnode.testnet.sui.io:443
